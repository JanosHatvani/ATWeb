namespace Modules
{

    public enum PropertyTypes
    {
        Id,
        Name,
        LinkText,
        ClassName,
        CssSelector,
        TagName,
        Xpath,
        type,
        PartialLinkText
    }
}
